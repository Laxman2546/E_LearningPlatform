<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>htmlBasics</title>
    <link rel="stylesheet" href="../stylesheet/learning.css" />
    <link
      href="https://cdn.jsdelivr.net/npm/remixicon@4.5.0/fonts/remixicon.css"
      rel="stylesheet"
    />
  </head>
  <body>
    <%- include('userNavbar') %>
    <div class="wrapper">
      <div class="topBar">
        <div class="header">
          <div class="heading">
            <h1><%= course.title %></h1>
            <p><%= course.description %></p>
            <p class="instructor" style="font-size: 0.9rem">
              Instructor:
              <span style="text-decoration: underline"
                ><%= course.createdBy %></span
              >
            </p>
            <form action="/course/video" method="post">
              <input type="hidden" name="courseId" value="<%= courseId %>" />
              <input type="hidden" name="title" value="<%= course.title %>" />
              <div class="button">
                <button type="submit">Start Learning</button>
              </div>
            </form>
          </div>
          <div class="sidebar">
            <div class="video">
              <img src="<%= course.courseImage %>" alt="" />
            </div>
          </div>
        </div>
      </div>
      <div class="middleBar">
        <div class="rating">
          <div class="ratingT">
            <i class="ri-star-fill" style="color: #7808d0"></i>
            <% if (!course.rating) { %>
            <p>no course found</p>
            <% } else{ %>
            <h3><%= courses.rating %></h3>
            <% } %>
          </div>
          <div class="reviews">
            <p>
              <span id="review"></span>
              <span style="text-decoration: underline">reviews </span>
            </p>
          </div>
        </div>
        <div class="expreience">
          <h3>Begginer level</h3>
          <p>No prior expreience required</p>
        </div>
        <div class="duration">
          <h4>Duration</h4>
          <p><%= course.duration %></p>
        </div>
        <div class="schedule">
          <h3>Flexible scheduled</h3>
          <p>Learn at your own pace</p>
        </div>
      </div>
    </div>

    <script src="https://cdnjs.cloudflare.com/ajax/libs/gsap/3.12.5/gsap.min.js"></script>
    <script src="../scripts/learning.js"></script>
  </body>
</html>
