<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Comment Section</title>
    <link rel="stylesheet" href="/stylesheet/commentSection.css" />
  </head>
  <body>
    <div class="comments-container">
      <div class="question-header">
        <div class="question-profile">
          <img
            src="<%= question.profileImage ? question.profileImage : '/images/default.png' %>"
            alt="Questioner's Profile"
            class="questioner-pic"
          />
          <div>
            <h2 class="question-title"><%= question.title %></h2>
            <p class="question-meta">
              <span class="question-user">
                Posted by: <strong><%= question.userName %></strong>
              </span>
              <span class="question-date"> | Date: <%= question.date %></span>
            </p>
          </div>
        </div>
        <p class="question-description"><%= question.description %></p>
      </div>
      <div class="add-comment-section">
        <img
          src="<%= user.profileImage ? user.profileImage : '/images/default.png' %>"
          alt="Your Profile Picture"
          class="profile-pic"
        />
        <form action="/community/postcomments" method="POST">
          <input type="hidden" name="questionId" value="<%= question._id %>" />
          <textarea
            name="commentText"
            class="comment-input"
            placeholder="Share your thoughts..."
            required
          ></textarea>
          <button type="submit" class="submit-comment-btn">Post</button>
        </form>
      </div>
      <div class="comments-list">
        <% if (comments && comments.length > 0) { %> <%
        comments.forEach((comment) => { %>
        <div class="comment-item">
          <img
            src="<%= comment.profileImage ? comment.profileImage : '/images/default.png' %>"
            alt="User Profile"
            class="comment-pic"
          />
          <div class="comment-body">
            <div class="comment-header">
              <span class="user-name"><%= comment.userName %></span>
              <span class="comment-timestamp"><%= comment.createdAt %></span>
            </div>
            <p class="comment-text"><%= comment.commentText %></p>
            <div class="comment-actions">
              <button class="like-btn">Like</button>
              <button class="reply-btn">Reply</button>
            </div>
          </div>
        </div>
        <% }) %> <% } else { %>
        <p>No comments yet.</p>
        <% } %>
      </div>
    </div>
  </body>
</html>
